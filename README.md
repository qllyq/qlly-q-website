# QLLY_Q clean cockpit edition

IMPORTANT: No AI-generated cockpit image is used.

Hero source:
- `hero-cockpit-cut.jpeg` = the user's manually edited cockpit image.
- `hero-cockpit-window-transparent.png` = a transparent derivative of that exact user image, with only the black window area made transparent so `space.mp4` can appear behind it.
- `space.mp4` = the space video.

Upload all files to the GitHub repository root.
